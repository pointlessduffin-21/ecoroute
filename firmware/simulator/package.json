{
  "name": "simulator",
  "private": true,
  "scripts": {
    "start": "node --import tsx/esm simulate.ts",
    "start:bun": "bun simulate.ts",
    "start:once": "node --import tsx/esm simulate.ts --once",
    "start:once:bun": "bun simulate.ts --once"
  },
  "dependencies": {
    "mqtt": "^5.10.1"
  },
  "devDependencies": {
    "@types/node": "^25.4.0",
    "tsx": "^4.19.2",
    "typescript": "^5.8.2"
  }
}
